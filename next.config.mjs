/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: false,
    images: {
        domains: ['horizon-tailwind-react-git-tailwind-components-horizon-ui.vercel.app','i.stack.imgur.com'],

      },
};

export default nextConfig;
